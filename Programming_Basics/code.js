// This variable is called LikeCount and starts ar zero
var likeCount = 0;
// the function below is caled increaseLikes()
function increaseLikes() {
    // Inside the function has an operation that adds the variable by one 
    likeCount = likeCount + 1;
}
